+++
title = "SwiftUI没有正常显示桌面图标AppIcon"
date = 2025-02-11
authors = ["小中"]
[taxonomies]
tags = ["swiftUI"]

+++

快速绘制AppIcon图标：下载App：`Bakery`，不过不建议在生产环境下使用。

如果你把生成的AppIcon图标拉进`Assets`文件夹，但是不能正常显示，可以按照以下方法。

首先，`cmd + shift + k` 清除缓存，

`cmd + b`或者`cmd + r`重新build一次项目即可。
